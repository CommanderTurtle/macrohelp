--- a/Task.h
+++ b/Task.h
@@ -16,6 +16,7 @@ public:
     int appendAction(const std::shared_ptr<AbstractAction> &act);
 
     friend class TaskThread;
     friend class TaskTab;
     friend class TaskTabsManager;
+    friend class TaskExecutor;   // <-- added by tasket-httpd sidecar
 };
 
 #endif // TASK_H
